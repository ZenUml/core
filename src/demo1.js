// Basic functionality:
// Comments
// Starter
// Call a method
// Self-method
// Nested method
// Assignment
export default `
// PriceCal
// SFCal, GSTCal < FeeCal()
// 
priceCal = new PriceCal(sfCal, gstCal) {
  calculators = from(sfCal, gstCal)
}

"priceCal:PriceCal".getTotal(order) {
  // total = price
  forEach("cal in calculatorssdfasdfsasdfsdfsdf") {
    extraFee = cal.cal(order)
    if(price > 100){
   test.mv
 }    total.add(extraFee)
  }
  return total
}`
