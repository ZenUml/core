// Basic functionality:
// Comments
// Starter
// Call a method
// Self-method
// Nested method
// Assignment
export default `
Client->Controller.GetOrder(id) {
  // \`DELETE orders/id\`
  OrderService.Get(id) {
    if(x) {
      ===="Not found"====
      OrderRepo."load order by id" {
      ====="Not found"=====
        MF.load
      ====="Not found"=====
        return 0
      }
    } else {
    == "do something else" ==
      Rep.x

    }
    =="Not found"==
    return order
  }
  return response
}`
