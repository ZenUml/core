// Basic functionality:
// Comments
// Starter
// Call a method
// Self-method
// Nested method
// Assignment
export default `
A
B
@Starter(Macro)
commentIds = LoadComments() {
  if(x) {
    Confluence.getContentProerpty()
  }
}

foreach(commentId) {
  comment = Confluence.getComment(commentId)
  
  Render(comment) {
    // if(commentIsOpen) {
    //   show()
    // }
  }
  
}
`
